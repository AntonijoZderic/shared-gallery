Make sure you have Apache configured to allow .htaccess files.

Create database and edit config.php file.

Activate these extensions in your php.ini configuration file:
- extension=pdo_mysql
- extension=fileinfo